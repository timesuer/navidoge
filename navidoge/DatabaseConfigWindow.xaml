<Window x:Class="navidoge.DatabaseConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="数据库配置管理" Height="520" Width="720"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip">
    
    <Window.Resources>
        <Style x:Key="InputStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="LabelStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,0,0,3"/>
            <Setter Property="Foreground" Value="#333"/>
        </Style>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧配置列表 -->
        <Border Grid.Column="0" BorderBrush="#DDD" BorderThickness="1" Margin="0,0,10,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="已保存的配置" FontWeight="Bold" FontSize="14" 
                           Margin="10" Foreground="#333"/>

                <ListBox Grid.Row="1" x:Name="ConfigListBox"
                         ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile}"
                         BorderThickness="0"
                         Padding="5">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="5">
                                <TextBlock Text="{Binding Alias}" FontWeight="SemiBold" 
                                           Foreground="#333"/>
                                <TextBlock Foreground="#666" FontSize="11">
                                    <Run Text="{Binding Host}"/>
                                    <Run Text=":"/>
                                    <Run Text="{Binding Port}"/>
                                    <Run Text=" / "/>
                                    <Run Text="{Binding Database}"/>
                                </TextBlock>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="10">
                    <Button Content="➕ 新增" Click="AddConfig_Click" 
                            Style="{StaticResource ButtonStyle}" Background="#4CAF50"/>
                    <Button Content="🗑️ 删除" Click="DeleteConfig_Click" 
                            Style="{StaticResource ButtonStyle}" Background="#F44336"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- 右侧编辑区域 -->
        <Border Grid.Column="1" BorderBrush="#DDD" BorderThickness="1" Padding="15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto">
                    <StackPanel IsEnabled="{Binding HasSelection}">
                        <TextBlock Text="配置详情" FontWeight="Bold" FontSize="14" 
                                   Margin="0,0,0,15" Foreground="#333"/>

                        <TextBlock Text="别名/备注 *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="AliasBox" Text="{Binding EditingProfile.Alias, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputStyle}"/>

                        <TextBlock Text="主机地址 *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="HostBox" Text="{Binding EditingProfile.Host, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputStyle}"/>

                        <TextBlock Text="端口 *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="PortBox" Text="{Binding EditingProfile.Port, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputStyle}"/>

                        <TextBlock Text="数据库名" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="DatabaseBox" Text="{Binding EditingProfile.Database, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputStyle}"/>

                        <TextBlock Text="用户名" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="UsernameBox" Text="{Binding EditingProfile.Username, UpdateSourceTrigger=PropertyChanged}" 
                                 Style="{StaticResource InputStyle}"/>

                        <TextBlock Text="密码" Style="{StaticResource LabelStyle}"/>
                        <PasswordBox x:Name="PasswordBox" Padding="8,6" Margin="0,0,0,10"
                                     BorderBrush="#CCC" BorderThickness="1"
                                     PasswordChanged="PasswordBox_PasswordChanged"/>
                    </StackPanel>
                </ScrollViewer>

                <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                    <Button Content="💾 保存配置" Click="SaveConfig_Click" 
                            Style="{StaticResource ButtonStyle}" Background="#4CAF50"/>
                    <Button Content="关闭" Click="Close_Click" 
                            Style="{StaticResource ButtonStyle}" Background="#757575"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
